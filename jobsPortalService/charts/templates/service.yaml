apiVersion: v1
kind: Service
metadata:
  labels:
    app: jobportal-service
  name: jobportal-service
spec:
  ports:
    - name: http
      port: 4000
      targetPort: 4000
  selector:
    app: jobportal-service
  type: ClusterIP